## Setting up python

Check using the virutal environment Python:

```bash
which python
# <directory_path>/.venv/bin/python
```

If not, activate the virtual environment:

```bash
source .venv/bin/activate
```